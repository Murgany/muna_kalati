version: 1.0

runtime: python311 

build:
  commands:
    - python -m ensurepip --upgrade
    - python -m pip install --upgrade pip
    - pip install --no-cache-dir -r muna_kalati/requirements.txt

run:
  command: gunicorn --bind 0.0.0.0:8000 config.wsgi:application

network:
  port: 8000

