version: 1.0
runtime: python3
build:
  commands:
    - pip install -r requirements.txt
    - python manage.py collectstatic --noinput
run:
  command: gunicorn confil.wsgi:application --bind 0.0.0.0:8080
  network:
    port: 8080